'use strict';

describe('Services: AreaSelection', function() {
  beforeEach(module('frontendApp'));

  var AreaSelection;

  beforeEach(function() {
    inject(function($injector) {
      AreaSelection = $injector.get('AreaSelection');
    });
  });

  it('should instantiate empty selection models', function() {
  });

  it('should allow adding one or more areas', function() {
  });

  it('should allow adding one or more districts', function() {
  });

  it('should allow adding one or more regions', function() {
  });

  it('should clear selection', function() {
  });

});
